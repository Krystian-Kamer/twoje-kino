model Movie {
  id           Int    @id @default(autoincrement())
  title        String
  description  String
  duration     Int
  cinemaId     Int
  videoUrl     String
  thumbnailUrl String
  cinema       Cinema @relation(fields: [cinemaId], references: [id])
}
